<script>
  import InputSelect from './InputSelect.svelte'
  const { children, ...props } = $props();
  const {
    is = 'ds-select-autocomplete',
    textNoResults = 'No results.',
  } = props
</script>

<InputSelect {...props} {is} data-i18n-no-results={textNoResults}>{@render children?.()}</InputSelect>
